# Tests (planned)

Future unit tests for transforms/history, integration tests for document persistence and widgets, and browser smoke/visual tests. No test runner is configured yet. See [`../plan.md`](../plan.md).
