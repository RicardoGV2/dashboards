# Web shell

Runnable TypeScript/Vite editor. Run `npm run dev` at the repository root. Composes portable document/camera/history modules, a DOM renderer, browser input and IndexedDB. Built assets are in `dist/` with `/dashboards/` base path. Does not deploy or reuse root sample files. See the root README and plan for limitations.
