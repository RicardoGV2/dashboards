# Document model (planned)

Versioned canvas nodes, edges, groups, transforms, datasets, assets, serialization, validation and migrations. No implementation yet. See [`../../plan.md`](../../plan.md).
