# Shared UI (planned)

React toolbars, panels, inspector, layers and accessibility primitives. UI depends on engine interfaces; the engine does not depend on React. No implementation yet. See [`../../plan.md`](../../plan.md).
